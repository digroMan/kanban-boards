import cn from 'classnames'
import styles from './TaskDelete.module.scss'
import type { TBoardProps } from './TaskDelete.props'

export const TaskDelete = ({title} : TBoardProps) => {
    return (
        <div className={styles.container}>
            <ul>

            </ul>
        </div>
    )
}